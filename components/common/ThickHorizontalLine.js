import React from "react";
import Box from "../common/Box";

const ThickHorizontalLine = (props) => (
  <Box borderTopWidth={4} borderColor="lightBorder" {...props} />
);

export default ThickHorizontalLine;
