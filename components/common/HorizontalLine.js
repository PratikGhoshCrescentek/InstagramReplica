import React from "react";
import {StyleSheet} from "react-native"
import Box from "../common/Box";

const HorizontalLine = (props) => (
  <Box borderTopWidth={1} borderColor="lightBorder" {...props} />
);

export default HorizontalLine;
